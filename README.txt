Requires PostgreSQL.
Make this program its own database as it repeatedly creates and drops tables.
Script location must be in the SQL server data directory.
To run, use the command "psql -q -d [database_name] -f init.sql"
Script syntax is as follows:
command,argument
See runner.sql for commands.
